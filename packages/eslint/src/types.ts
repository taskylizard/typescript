export interface ConfigOptions {
  /** TypeScript support. */
  typescript?: boolean;
  /** Markdown support. */
  markdown?: boolean;
  /** Jest support. */
  jest?: boolean;
  /** React support. */
  react?: boolean;
  /** Astro support. */
  astro?: boolean;
}
